# Paul's Personal Website

## Steps for a local instance
- Install `nodejs`, and `pnpm`
- Run `pnpm install`
- Start dev instance `pnpm run dev`

## Other scripts
- `pnpm run lint` — run ESLint checks

